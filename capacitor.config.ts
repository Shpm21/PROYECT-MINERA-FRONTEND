import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PROYECT-MINERA-FRONTEND',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
